#include <iostream>
#include <string>

using namespace std;

int main()
{
	string str = "the cat and the dog";
	int count = 0;
	size_t pos = str.find("the");

	while (pos != string::npos)
	{
		count++;
		pos = str.find("the", pos + 1);
	}
	cout << count << endl;
}
